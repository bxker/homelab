apiVersion: v2
name: prometheus-stack
version: 1.0.0
description: kube-prometheus-stack wrapper (Prometheus + Grafana)
dependencies:
  - name: kube-prometheus-stack
    version: "65.*"
    repository: https://prometheus-community.github.io/helm-charts
